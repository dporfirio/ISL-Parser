# Simple test with one goal.
import food_assembly
labels

	moving: [
		  predicate: entity_in,
		  params: [sauron, table]
		 ]


endlabels

module

	st: [0: init, 1: moving];

	[] 0 -> 1;

endmodule

options

	conditional_effects;

endoptions