# Simple test with one action.
import general
labels

	go: [
		  action: move_to,
		  params: [sauron, kitchen]
		 ]


endlabels

module

	st: [0: init, 1: go];

	[] 0 -> 1;

endmodule

options

	conditional_effects;

endoptions